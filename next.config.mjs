/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['i.pinimg.com','static.vecteezy.com','store-rvovlarb.saleor.cloud'], // Specify allowed external image domains here

      },
};

export default nextConfig;
